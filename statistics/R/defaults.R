paramDflts <- list(
                   area_unit = "mm",
                   calc_unit = "FOV_ID",
                   cell_dive_id = NULL,
                   force_recalc = FALSE,
                   neighborhood_radius = 30,
                   positive_cutoff = 0.95,
                   min_microenv_cells = 1, 
                   number_threads = 1
                  )

